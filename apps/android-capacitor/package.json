{
  "name": "sexta-android",
  "version": "1.4.0",
  "private": true,
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:prepare": "node scripts/prepare-android.mjs && node scripts/prepare-actions.mjs && node scripts/prepare-live-tools.mjs && node scripts/optimize-android-live.mjs && node scripts/stabilize-android-runtime.mjs",
    "android:open": "cap open android",
    "android:debug": "npm run android:sync && npm run android:prepare && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0",
    "@capacitor/push-notifications": "8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "typescript": "^5.9.0"
  }
}
